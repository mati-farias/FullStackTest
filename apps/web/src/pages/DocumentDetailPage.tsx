export function DocumentDetailPage(): JSX.Element {
  // TODO: Extract :id from URL params with useParams().
  //       Fetch the document using the useDocument hook.
  //       Render document details: title, status, content, reviewer.
  //       If document.status === 'DRAFT' and the current user is the author,
  //       render DocumentForm for inline editing.
  //       Render TransitionButton for available transitions.
  //       Render EventHistory with the document's event history.
  //       Handle loading and error states.
  throw new Error('Not implemented')
}
