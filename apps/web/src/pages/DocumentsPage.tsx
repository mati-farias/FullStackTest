export function DocumentsPage(): JSX.Element {
  // TODO: Fetch the document list using the useDocuments hook.
  //       Render a list of DocumentCard components.
  //       Show a "New document" button that opens a DocumentForm to create a draft.
  //       Add a status filter (dropdown or tab strip) that updates the query.
  //       Handle loading and error states gracefully.
  throw new Error('Not implemented')
}
