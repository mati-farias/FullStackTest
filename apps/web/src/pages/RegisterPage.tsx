export function RegisterPage(): JSX.Element {
  // TODO: Render a registration form with fields: email, password, name,
  //       and role (select: AUTHOR / REVIEWER / ADMIN, default AUTHOR).
  //       On submit, POST to /api/auth/register via apiClient.
  //       On success, call useAuthStore.login(payload) and navigate to /documents.
  //       Show an error message on failure.
  throw new Error('Not implemented')
}
