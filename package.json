{
  "name": "document-approval-workflow",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w packages/shared",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5"
  }
}
